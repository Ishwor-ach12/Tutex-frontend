import React from 'react'
import { ScrollView, Text } from 'react-native'

const Analytics = () => {
  return (
    <ScrollView>
      <Text>Analytics</Text>
    </ScrollView>
  )
}

export default Analytics